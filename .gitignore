.venv/
__pycache__/
*.pyc
.DS_Store
# Saved editing drafts (PDF snapshots + edit state) — kept on disk forever,
# but not committed (PDF blobs would bloat git). Delete this line to track them.
drafts/
